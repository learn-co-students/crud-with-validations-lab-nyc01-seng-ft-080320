<h1>List of all songs:</h1>

<table>
    <!-- <tr>
        <th> Song </th>
        <th> Artist </th>
        <th> Genre </th>
    </tr> -->
    <% @songs.each do |song| %>
    <tr>
        <td> <%= song.title %> </td>
        <td> <%= song.artist_name %> </td>
        <td> <%= song.genre %> </td>
    </tr>
    <% end %>
</table>
